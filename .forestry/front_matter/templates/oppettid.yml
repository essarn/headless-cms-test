---
label: Öppettid
hide_body: true
fields:
- name: items
  type: field_group_list
  fields:
  - name: days
    type: text
    config:
      required: true
    label: Days
  - name: hours
    type: text
    config:
      required: true
    label: Hours
  config:
    min: 
    max: 
    labelField: 
  label: items
